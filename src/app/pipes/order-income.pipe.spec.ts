import { OrderIncomePipe } from './order-income.pipe';

describe('OrderIncomePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderIncomePipe();
    expect(pipe).toBeTruthy();
  });
});
