import { IncomeExpenseTransformPipe } from './income-expense-transform.pipe';

describe('IncomeExpenseTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeExpenseTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
